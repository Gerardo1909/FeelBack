"""
    Configuración del proyecto como paquete.
"""

from setuptools import setup, find_packages

setup(
    name="FeelBackApp",
    version="0.1",
    packages=find_packages(),
    install_requires=[]
)